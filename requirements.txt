# Core numerics & imaging
numpy>=1.24.0
Pillow>=10.0.0
opencv-python>=4.8.0

# Visualization for the demo (saliency overlay, palette strip)
matplotlib>=3.7.0

# Testing
pytest>=8.0.0

# YOLOv8 object detection
ultralytics>=8.0.0

# CLIP embeddings (open_clip)
open-clip-torch>=2.24.0

# PyTorch (needed for CLIP and to accelerate on CUDA/MPS if available)
torch>=2.2.0
torchvision>=0.17.0
# torchaudio is optional; include if you later add audio features
# torchaudio>=2.2.0
